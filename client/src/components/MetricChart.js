import React from "react";

const MetricChart = () => {
  //need some logic to pull either user stats or global stats depending on url
  return <div>{/* implement chartJS here with fetched data */}</div>;
};

export default MetricChart;
